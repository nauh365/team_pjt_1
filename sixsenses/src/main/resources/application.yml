#port
server:
  port: 8089

spring:
  security:
    oauth2:
      client:
        provider:
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            # Access Token 요청 URL
            token-uri: https://nid.naver.com/oauth2.0/token
            # 사용자 정보 조회 URL
            user-info-uri: https://openapi.naver.com/v1/nid/me
            # 응답받은 사용자 정보 중 사용자 이름이 무엇인지 담겨있는 JSON Key
            user-name-attribute: response
        #          kakao:
        #            authorization-uri: https://kauth.kakao.com/oauth/authorize
        #            token-uri: https://kauth.kakao.com/oauth/token
        #            user-info-uri: https://kapi.kakao.com/v2/user/me
        #            user-name-attribute: id

        registration:
          #          kakao:
          #            client-id: 6e64f35e379d85a7ca58789e1a3fce27
          #            client-secret: lTTsFXcUx57Ub5iRLFMIgUBVusHtcbhY
          #            redirect-uri: http://localhost:8089/login/oauth2/code/kakao
          #            authorization-grant-type: authorization_code
          #            client-authentication-method: client_secret_post
          #            client-name: Kakao
          #            scope:
          #              - profile_nickname
          #              - profile_image
          #              - account_email
          naver:
            client-id: s6DVMwODRPAhXl2h_RZX
            client-secret: ZRQP9K9epB
            # CallbackURL
            redirect-uri: http://localhost:8089/login/oauth2/code/naver
            authorization-grant-type: authorization_code
            client-authentication-method: client_secret_post
            client-name: Naver
            # 쓰기로 체크한 권한들 + scope는 꼭 작성 안해도 됨
            scope:
              - name
              - email
              - nickname
              - profile_image
              - mobile

  datasource:
    url:  jdbc:mysql://book.cnqc8ga2irz8.ap-northeast-2.rds.amazonaws.com:3306/sixsenses
    driver-class-name: com.mysql.cj.jdbc.Driver
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    com.likelion.sixsenses: debug

jwt:
  secret: aaaabbbsdifqbvaesoioegwaaaabbbsdifqbvaesoioegwaaaabbbsdifqbvaes

